cargo-features = ["profile-rustflags", "trim-paths"]

[package]
name = "cloud_loader"
version = "0.1.0"
edition = "2021"
authors = [
    "Ivenin Valentin <iveninvala7@gmail.com>",
]
description = "Backend for a cross-platform desktop application with Tauri."
repository = "https://github.com/IOSHED/Iosh__CloudLoader"

[lib]
name = "cloud_loader_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2.0.0-beta", features = [] }

[dependencies]
core-cloud-loader = { path = "../core-cloud-loader" }
tauri = { version = "2.0.0-beta", features = ["config-toml", "devtools"] }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1.35.1", features = ["full"] }
tauri-plugin-shell = "2.0.0-beta"
serde_json = "1"
tauri-plugin-fs = "2.0.0-beta.10"
futures = "0.3.30"
thiserror = "1"

[profile.dev]
incremental = true          # Compile your binary in smaller steps.
rustflags = ["-Zthreads=8"] # Better compile performance.

[profile.release]
codegen-units = 1                            # Allows LLVM to perform better optimization.
lto = true                                   # Enables link-time-optimizations.
opt-level = "s"                              # Prioritizes small binary size. Use `3` if you prefer speed.
panic = "abort"                              # Higher performance by disabling panic handlers.
strip = true                                 # Ensures debug symbols are removed.
trim-paths = "all"                           # Removes potentially privileged information from your binaries.
rustflags = ["-Cdebuginfo=0", "-Zthreads=8"] # Better compile performance.
